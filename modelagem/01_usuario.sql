/*
  Usuario default para acesso inicial:
  username: admin
  password: admin123
*/

CREATE TABLE TODO_LIST.USUARIO (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY
        MINVALUE 1
        MAXVALUE 9999999999999999999999999999
        INCREMENT BY 1
        START WITH 1
        CACHE 20
        NOORDER
        NOCYCLE
        NOKEEP
        NOSCALE
        NOT NULL ENABLE,
    USERNAME VARCHAR2(100 BYTE) NOT NULL ENABLE,
    NAME VARCHAR2(150 BYTE),
    PASSWORD VARCHAR2(255 BYTE) NOT NULL ENABLE,
    CREATED_AT TIMESTAMP(6) DEFAULT CURRENT_TIMESTAMP NOT NULL ENABLE,
    UPDATED_AT TIMESTAMP(6),
    CONSTRAINT PK_USUARIO PRIMARY KEY (ID),
    CONSTRAINT UK_USUARIO_USERNAME UNIQUE (USERNAME)
);

INSERT INTO TODO_LIST.USUARIO (
    USERNAME,
    NAME,
    PASSWORD
) VALUES (
    'admin',
    'Administrador',
    '$2a$12$D99.RG7TIekoNjIz5ISrGe28L/8.q5JtcygE0UhVnWVkdAzdZ.nk6'
);

CREATE TABLE TODO_LIST.REFRESH_TOKEN (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY
        MINVALUE 1
        MAXVALUE 9999999999999999999999999999
        INCREMENT BY 1
        START WITH 1
        CACHE 20
        NOORDER
        NOCYCLE
        NOKEEP
        NOSCALE
        NOT NULL ENABLE,
    TOKEN VARCHAR2(255 BYTE) NOT NULL ENABLE,
    SESSION_ID VARCHAR2(255 BYTE) NOT NULL ENABLE,
    USUARIO_ID NUMBER NOT NULL ENABLE,
    EXPIRES_AT TIMESTAMP(6) NOT NULL ENABLE,
    REVOKED_AT TIMESTAMP(6),
    CREATED_AT TIMESTAMP(6) DEFAULT CURRENT_TIMESTAMP NOT NULL ENABLE,
    CONSTRAINT PK_REFRESH_TOKEN PRIMARY KEY (ID),
    CONSTRAINT UK_REFRESH_TOKEN UNIQUE (TOKEN),
    CONSTRAINT UK_REFRESH_TOKEN_SESSION UNIQUE (SESSION_ID),
    CONSTRAINT FK_REFRESH_TOKEN_USUARIO FOREIGN KEY (USUARIO_ID)
        REFERENCES TODO_LIST.USUARIO (ID)
);

CREATE TABLE TODO_LIST.BLACKLISTED_TOKEN (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY
        MINVALUE 1
        MAXVALUE 9999999999999999999999999999
        INCREMENT BY 1
        START WITH 1
        CACHE 20
        NOORDER
        NOCYCLE
        NOKEEP
        NOSCALE
        NOT NULL ENABLE,
    TOKEN VARCHAR2(2000 BYTE) NOT NULL ENABLE,
    EXPIRES_AT TIMESTAMP(6) NOT NULL ENABLE,
    CREATED_AT TIMESTAMP(6) DEFAULT CURRENT_TIMESTAMP NOT NULL ENABLE,
    CONSTRAINT PK_BLACKLISTED_TOKEN PRIMARY KEY (ID),
    CONSTRAINT UK_BLACKLISTED_TOKEN UNIQUE (TOKEN)
);
